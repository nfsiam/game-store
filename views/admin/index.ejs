<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous"> 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <title>Wallet Recharge Requests</title>
</head>
<body>
    <div class="empty mt-3"></div>
    <a href="/logout" class="ml-2"  style="padding: 7px; border: 1px solid transparent; border-radius: 4px; color: white; background-color: rgb(163, 43, 127);">Logout</a>
    <div class="container">
        
        <h1 class="font-weight-light ml-2 mt-4">List of wallet recharge requests</h1>
        <table class="table mt-4">
            <th>
                id
            </th>
            <th>
                username
            </th>
            <th>
                approvedby
            </th>
            <th>
                amount
            </th>
            <th>
                status
            </th>
            <th>
                action
            </th>

            <% for(var i=0; i<result.length ;i++ ) { %>

                <tr>
                    <td><%= result[i].id %></td>
                    <td><%= result[i].username %></td>
                    <td><%= result[i].approvedby %></td>
                    <td><%= result[i].amount %></td>
                    <td><%= result[i].status %></td>
                    <td>
                        <% if(result[i].status=='pending'){ %>
                            <a href="home/approve/<%= result[i].id %>"  style="padding: 7px; border: 1px solid transparent; border-radius: 4px; color: white; background-color: rgb(44, 137, 212);">Approve</a> 

                            <a class="ml-2" style="padding: 7px; border: 1px solid transparent; border-radius: 4px; color: white; background-color: rgb(224, 62, 62);" href="home/reject/<%= result[i].id %>">Reject</a>

                        <% } %>
                    </td>
                 </tr>

            <% }   %>
           
        </table>


    </div>
</body>
</html>